import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inputView: {
    marginBottom: 15,
  },
  dateInputView: {
    display: 'flex',
    flexDirection: 'row',
  },
});

export {styles};
