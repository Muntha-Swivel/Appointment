import {StyleSheet} from 'react-native';
import {DEVICE_WIDTH} from 'shared';
const styles = StyleSheet.create({
  aCardView: {
    marginBottom: 15,
  },
  segmentButtons: {
    margin: 20,
    width: DEVICE_WIDTH - 10,
  },
});

export {styles};
