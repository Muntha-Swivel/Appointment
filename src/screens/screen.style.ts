import {StyleSheet} from 'react-native';

const commonScreenStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export {commonScreenStyle};
